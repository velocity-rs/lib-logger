[package]
name = "log"
version = "0.1.0"
edition = "2024"

[lib]

[dependencies]
log = { version = "0.4.27" }
fern = { version = "0.7.1", features = ["colored"] }
chrono = { version = "0.4.42" }

[lints.rust]
unsafe_code = "forbid"
